#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    int tree;
    int n,q,i,a;
    scanf("%d%d",&n,&q);
    for(i=0;i<q;i++)
    {
        scanf("%d",&a);
        if(a==0) {scanf("%d%d%d",&tree[1],&tree[2],&tree[3]);}
        else scanf("%d%d",&tree[1],&tree[2]);
    }
    return 0;
}
